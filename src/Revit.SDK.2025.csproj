<Project Sdk="Microsoft.NET.Sdk" ToolsVersion="Current">
	<PropertyGroup>
		<RevitVersion>2025</RevitVersion>
		<Version>$(RevitVersion).4.0.32</Version>
		<FileVersion>$(RevitVersion).4.0</FileVersion>
		<AssemblyVersion>$(RevitVersion).4.0</AssemblyVersion>
		<TargetPath>net8.0</TargetPath>
		<IsPackable>true</IsPackable>
	</PropertyGroup>

	<Import Project="Revit.SDK.Nuget.csproj" />
</Project>